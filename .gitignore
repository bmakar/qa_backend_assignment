target/
*.iml
.idea/
.vscode/
*.log
.DS_Store
.claude
# Local GitLab credentials for src/main/resources - copy sample.gitlab.properties to create
# this file, and never commit the real one: it can hold a personal access token.
gitlab.properties

# Allure. Results live under target/, but the CLI and the gh-pages action write here too.
allure-results/
allure-report/
allure-history/
.allure/
